import React from 'react';

/**
 * HeroBgAnimation — now a transparent passthrough.
 * The full-page Vanta Birds background is handled by VantaBackground in App.js
 * and shows through here naturally. This component preserves the existing
 * HeroSection structure without breaking changes.
 */
const HeroBgAnimation = () => null;

export default HeroBgAnimation;